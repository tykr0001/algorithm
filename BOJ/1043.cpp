#include <bits/stdc++.h>

using namespace std;

// union find 직접구현해보기

vector<vector<int>> party;
vector<int> know;

int main(void){
    int N, M;
    cin >> N >> M;
    party.resize(M);
    int know_size;
    cin >> know_size;
    know.resize(inp);
    for(int i=0; i<know_size; i++){
        int inp;
        cin >> inp;
        know[i] = inp;
    }
    for(int i=0; i<M; i++){
        int inp;
        cin >> inp;
        know[i] = inp;
    }
    return 0;
}